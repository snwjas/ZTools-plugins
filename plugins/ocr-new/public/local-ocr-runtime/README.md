# Local OCR Runtime

This folder is copied into the ZTools plugin build. On Windows, `scripts/build-local-ocr-runtime.ps1` creates `win32/rapidocr-server/rapidocr-server.exe` here.

When the executable exists, the plugin starts it directly and does not require the end user to install Python.